import {
  this.parameter {parameter},
  this.table {encoder-error, table},
}

data command(a) {
| Command(
    template: &text,
    parameters: list(pair(&text, parameter)),
    result-encoder: (&table) -> except(encoder-error, a),
  )
}

define void-encoder(_: &table): except(encoder-error, unit) {
  OK(Unit)
}

define statement(t: &text): command(unit) {
  Command of {
    template = t,
    parameters = [],
    result-encoder = void-encoder,
  }
}
