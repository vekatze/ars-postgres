import {
  core.c-string {c-string, from-string},
  core.debug {panic},
  core.string {format},
  this._c-string {from-pointer},
  this.foreign {CONNECTION_OK},
}

foreign {
  PQconnectdb(pointer) -> pointer,
  PQstatus(pointer) -> int,
  PQfinish(pointer) -> void,
  PQhost(pointer) -> pointer,
  PQport(pointer) -> pointer,
  PQdb(pointer) -> pointer,
  PQuser(pointer) -> pointer,
  PQpass(pointer) -> pointer,
  PQerrorMessage(pointer) -> pointer,
}

data conninfo {
| Conn-Info(
    host: string,
    port: string,
    dbname: string,
    user: string,
    password: string,
  )
}

data connection-error {
| Connection-Error(message: string)
}

nominal {
  data connection,
  resource _connection,
  define connect(c: conninfo) -> either(connection-error, connection),
}

resource _connection {
  (ptr: int) => {
    let ptr = magic cast(int, pointer, ptr);
    let _ = magic external PQfinish(ptr);
    Unit
  },
  (ptr: int) => {
    let ptr = magic cast(int, pointer, ptr);
    let !host = from-pointer(magic external PQhost(ptr));
    let !port = from-pointer(magic external PQport(ptr));
    let !dbname = from-pointer(magic external PQdb(ptr));
    let !user = from-pointer(magic external PQuser(ptr));
    let password = from-pointer(magic external PQpass(ptr));
    let new-conn = connect(Conn-Info{host, port, dbname, user, password});
    match new-conn {
    | Right(c) =>
      magic cast(connection, int, c)
    | Left(_) =>
      pin message =
        format(
          "failed to clone the following connection:\nhost={} port={} dbname={} user={} password=<hidden>\n",
          List[host, port, dbname, user],
        );
      panic(message)
    }
  },
  -1,
}

data connection {
| _Connection(_connection)
}

define connect(c: conninfo) -> either(connection-error, connection) {
  let Conn-Info{host, port, dbname, user, password} = c;
  pin conn-string =
    format("host={} port={} dbname={} user={} password={}", List[
      host,
      port,
      dbname,
      user,
      password,
    ]);
  let conn-string = from-string(conn-string);
  let conn on conn-string = magic external PQconnectdb(magic cast(&c-string, pointer, conn-string));
  let _ = conn-string;
  let conn = magic cast(pointer, connection, conn);
  let status: int on conn = magic external PQstatus(magic cast(&connection, pointer, conn));
  if eq-int(status, CONNECTION_OK) {
    Right(conn)
  } else {
    let e on conn =
      Connection-Error(from-pointer(magic external PQerrorMessage(
        magic cast(&connection, pointer, conn),
      )));
    let _ = conn;
    Left(e)
  }
}
